presubmits:
  https://velostrata-internal-review.googlesource.com/velostrata-v2k:
  - name: presubmit-velostrata-internal-velostrata-v2k-csi-build
    always_run: true
    # Enables pod-utils, which inject init and sidecar containers into your pod.
    # init - checks out your PR correctly
    # sidecar - uploads your result to testgrid
    decorate: true
    spec: # This is a podspec, add any podspec fields below
      containers:
      # Bring any image, such as this cloud builder image.
      # No changes necessary for minimal testgrid integration!
      - image: eu.gcr.io/velos-auto-1/vls-builder-go:v0.2.5
        command:
        - ./csi/vlsdisk-csi-driver/build_local.sh
  - name: presubmit-velostrata-internal-velostrata-v2k-csi-ut
    decorate: true
    always_run: true
    spec:
      containers:
      - image: eu.gcr.io/velos-auto-1/vls-builder-go:v0.2.5
        command:
        - ./csi/vlsdisk-csi-driver/test/run_unit_local.sh
        args:
        - --dep
